# Notice

BookVoice Studio（书声工坊）基于 `epub2mp3` 项目改造升级，继续保留本地电子书转有声书的核心目标，并扩展桌面 GUI、章节预览、批量队列、MOBI/AZW3 实验支持、MP3 元数据、M4B 输出和更完整的环境检查。

当前代码包已迁移为 `bookvoice`，同时保留 `epub2mp3` 兼容入口，便于旧脚本逐步过渡。

Calibre `ebook-convert` 仅作为外部可选依赖使用，不随默认打包产物内置。重新分发 Calibre 时需要单独遵守 Calibre 的 GPL 许可证要求。
